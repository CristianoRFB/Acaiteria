'use client';

import { Check, ChefHat, Clock3, Copy, MessageCircle, PackageCheck, RefreshCw } from 'lucide-react';
import { httpsCallable } from 'firebase/functions';
import { collection, doc, getDocs, onSnapshot, query, where } from 'firebase/firestore';
import { useParams, useSearchParams } from 'next/navigation';
import { useCallback, useEffect, useRef, useState } from 'react';

import { PublicHeader } from '@/components/public-header';
import { Button } from '@/components/ui/button';
import { useCatalog } from '@/components/providers';
import { respondToOrderEditDirect, type PublicOrderEditProposal } from '@/lib/direct-orders';
import { getFirebaseClient, hasFirebaseConfig } from '@/lib/firebase/client';
import { formatBRL, getCustomerOrderStatusMessage, getOrderEstimateForStatus, type OrderStatus, type PricedItem } from '@/shared/domain';

interface PublicOrder {
  integrationMessage?: string; statusMessage?: string; publicCode?: string; orderNumber: string; createdAt?: string; updatedAt?: string; estimatedMinutes?: number;
  items: PricedItem[]; pricing: { totalCents: number }; fulfillment: { mode: 'PICKUP' | 'DELIVERY' }; status: OrderStatus; editProposal?: PublicOrderEditProposal;
}
const baseSteps: Array<{ statuses: OrderStatus[]; label: string; icon: typeof Clock3 }> = [
  { statuses: ['NEW'], label: 'Recebido', icon: Check }, { statuses: ['CONFIRMED'], label: 'Confirmado', icon: Clock3 }, { statuses: ['PREPARING'], label: 'Em preparo', icon: ChefHat }, { statuses: ['READY', 'OUT_FOR_DELIVERY'], label: 'Pronto', icon: PackageCheck }, { statuses: ['COMPLETED'], label: 'Concluído', icon: Check },
];
function rememberOrder(publicCode: string, orderNumber: string) { try { const saved = JSON.parse(localStorage.getItem('acai-mais-sabor-recent-orders') || '[]') as Array<{ publicCode: string; orderNumber: string; savedAt: number }>; localStorage.setItem('acai-mais-sabor-recent-orders', JSON.stringify([{ publicCode, orderNumber, savedAt: Date.now() }, ...saved.filter((item) => item.publicCode !== publicCode)].slice(0, 5))); } catch { /* opcional */ } }
async function copyText(value: string) { if (navigator.clipboard?.writeText) return navigator.clipboard.writeText(value); const input = document.createElement('textarea'); input.value = value; input.setAttribute('readonly', ''); input.style.position = 'fixed'; input.style.opacity = '0'; document.body.appendChild(input); input.select(); document.execCommand('copy'); input.remove(); }
function formatUpdatedAt(value: unknown): string | null { try { const date = value && typeof value === 'object' && 'seconds' in value ? new Date(Number((value as { seconds: unknown }).seconds) * 1000) : new Date(String(value)); if (Number.isNaN(date.getTime())) return null; return date.toLocaleString('pt-BR', { dateStyle: 'short', timeStyle: 'short' }); } catch { return null; } }

export default function OrderPage() {
  const { publicCode } = useParams<{ publicCode: string }>(); const search = useSearchParams(); const { config } = useCatalog();
  const routeCode = (() => { try { return decodeURIComponent(publicCode); } catch { return publicCode; } })();
  const [order, setOrder] = useState<PublicOrder | null>(null); const [loading, setLoading] = useState(true); const [error, setError] = useState(''); const [legacyMode, setLegacyMode] = useState(false); const [copied, setCopied] = useState(false); const [statusNotice, setStatusNotice] = useState(''); const [editBusy, setEditBusy] = useState(false); const [editError, setEditError] = useState(''); const previousStatus = useRef<OrderStatus | null>(null); const resolvedCodeRef = useRef(publicCode);
  const load = useCallback(async () => { if (!hasFirebaseConfig) { setError('Firebase não configurado.'); setLoading(false); return; } try { const { functions } = getFirebaseClient(); const lookup = httpsCallable<{ publicCode: string }, PublicOrder>(functions, 'getPublicOrder'); const response = await lookup({ publicCode: resolvedCodeRef.current }); setOrder(response.data); setError(''); } catch { setError('Pedido não encontrado ou temporariamente indisponível.'); } finally { setLoading(false); } }, []);
  useEffect(() => {
    resolvedCodeRef.current = routeCode;
    if (!hasFirebaseConfig) { void load(); return undefined; }
    const { db } = getFirebaseClient(); let stop = () => {}; let cancelled = false;
    async function subscribe() {
      let target = routeCode;
      const normalized = routeCode.replace(/^#/, '').toUpperCase();
      if (normalized.startsWith('A')) {
        try {
          const matches = await getDocs(query(collection(db, 'publicOrders'), where('orderNumber', '==', `#${normalized}`)));
          const match = matches.docs[0];
          if (!match) { if (!cancelled) { setLoading(false); setError('Pedido não encontrado. Confira o número e tente novamente.'); } return; }
          target = match.id;
        } catch { if (!cancelled) { setLoading(false); setError('Não foi possível procurar o pedido agora. Tente novamente.'); } return; }
      }
      if (cancelled) return;
      resolvedCodeRef.current = target;
      stop = onSnapshot(doc(db, 'publicOrders', target), (snapshot) => { if (snapshot.exists()) { setOrder(snapshot.data() as PublicOrder); setLoading(false); setError(''); setLegacyMode(false); } else { setLegacyMode(true); void load(); } }, () => { setLegacyMode(true); void load(); });
    }
    void subscribe();
    return () => { cancelled = true; stop(); };
  }, [load, routeCode]);
  useEffect(() => { if (!legacyMode) return undefined; const timer = setInterval(() => void load(), 8000); return () => clearInterval(timer); }, [legacyMode, load]);
  useEffect(() => { if (!order) return; rememberOrder(resolvedCodeRef.current, order.orderNumber); if (previousStatus.current && previousStatus.current !== order.status) { setStatusNotice(`Atualização: ${order.statusMessage ?? getCustomerOrderStatusMessage(order.status)}`); const timer = window.setTimeout(() => setStatusNotice(''), 7000); previousStatus.current = order.status; return () => window.clearTimeout(timer); } previousStatus.current = order.status; return undefined; }, [order]);
  async function respondToEdit(decision: 'ACCEPTED' | 'REJECTED') { if (!order?.editProposal || order.editProposal.status !== 'PENDING') return; setEditBusy(true); setEditError(''); try { await respondToOrderEditDirect(getFirebaseClient().db, resolvedCodeRef.current, decision); } catch (cause) { setEditError(cause instanceof Error ? cause.message : 'Não foi possível registrar sua decisão.'); } finally { setEditBusy(false); } }
  function whatsappUrl() { if (!order || !config.whatsappNumber) return '#'; const summary = order.items.map((item) => `${item.quantity}x ${item.productName} (${item.sizeLabel})`).join('\n'); const message = `Olá! Pedido ${order.orderNumber}\n${summary}\nTotal: ${formatBRL(order.pricing.totalCents)}\n${order.fulfillment.mode === 'PICKUP' ? 'Retirada' : 'Delivery'}`; return `https://wa.me/${config.whatsappNumber.replace(/\D/g, '')}?text=${encodeURIComponent(message)}`; }
  if (loading) return <main className="grid min-h-screen place-items-center bg-[#fffaf5]"><RefreshCw className="size-6 animate-spin text-[#82204f]" /></main>;
  if (!order) return <main className="min-h-screen bg-[#fffaf5]"><PublicHeader /><div className="mx-auto max-w-lg px-6 py-24 text-center"><h1 className="text-3xl font-black">Não encontramos esse pedido</h1><p className="mt-2 text-sm text-[#826a75]">{error}</p><Button className="mt-6 rounded-full bg-[#82204f] text-white" onClick={load}>Tentar novamente</Button></div></main>;
  const displayOrder = order.editProposal?.status === 'REJECTED' ? { ...order, items: order.editProposal.before.items, pricing: { ...order.pricing, totalCents: order.editProposal.before.pricing.totalCents }, fulfillment: { ...order.fulfillment, mode: order.editProposal.before.fulfillment.mode } } : order;
  const steps = order.fulfillment.mode === 'PICKUP' ? baseSteps.filter((step) => !step.statuses.includes('OUT_FOR_DELIVERY')) : baseSteps; const activeIndex = steps.findIndex((step) => step.statuses.includes(order.status)); const estimate = getOrderEstimateForStatus(order.status, order.estimatedMinutes ?? config.orderEstimateMinutes ?? 15); const orderCode = (order.orderNumber || publicCode).replace(/^#/, ''); const copyCode = async () => { try { await copyText(orderCode); setCopied(true); window.setTimeout(() => setCopied(false), 2200); } catch { setCopied(false); } }; const updated = order.updatedAt ? formatUpdatedAt(order.updatedAt) : null;
  return <main className="min-h-screen bg-[#fffaf5] text-[#2b1722]"><PublicHeader /><div className="mx-auto max-w-3xl px-4 py-10 sm:px-6">{order.editProposal && <section className={`mb-5 rounded-[28px] p-6 shadow-sm ${order.editProposal.status === 'PENDING' ? 'border-2 border-[#d7f04a] bg-[#fffde8]' : 'bg-white'}`}><p className="text-xs font-black uppercase tracking-wider text-[#a62c63]">Alteração no pedido</p><h2 className="mt-1 text-xl font-black">{order.editProposal.status === 'PENDING' ? 'A loja atualizou seu pedido' : order.editProposal.status === 'ACCEPTED' ? 'Alterações aceitas' : 'Alterações recusadas'}</h2><p className="mt-2 text-sm leading-relaxed text-[#6f5360]">{order.editProposal.status === 'PENDING' ? order.editProposal.summary : order.editProposal.status === 'ACCEPTED' ? 'Obrigado! A loja recebeu sua confirmação e seguirá com o pedido.' : 'Você recusou as alterações. A loja foi avisada e o pedido será restaurado à versão anterior.'}</p>{editError && <p role="alert" className="mt-3 text-sm font-bold text-red-700">{editError}</p>}{order.editProposal.status === 'PENDING' && <div className="mt-4 flex flex-col gap-2 sm:flex-row"><Button disabled={editBusy} onClick={() => void respondToEdit('ACCEPTED')} className="rounded-full bg-[#82204f] text-white">{editBusy ? 'Salvando…' : 'Concordo com as alterações'}</Button><Button disabled={editBusy} variant="outline" onClick={() => void respondToEdit('REJECTED')} className="rounded-full">Recusar alterações</Button></div>}</section>}<div className="rounded-[32px] bg-[#351924] p-6 text-white sm:p-9">{search.get('novo') === '1' && <span className="inline-flex items-center gap-2 rounded-full bg-[#d7f04a] px-3 py-1.5 text-xs font-black text-[#351924]"><Check className="size-3.5" /> Pedido salvo</span>}<p className="mt-5 text-sm text-white/60">Acompanhe seu pedido</p><div className="mt-1 flex flex-wrap items-center gap-3"><h1 className="text-4xl font-black tracking-[-.05em]">{order.orderNumber}</h1><button type="button" onClick={copyCode} className="inline-flex items-center gap-1.5 rounded-full bg-white/10 px-3 py-2 text-xs font-black text-white hover:bg-white/20" aria-label="Copiar código do pedido"><Copy className="size-3.5" /> {copied ? 'Código copiado!' : 'Copiar código'}</button></div><p className="mt-3 rounded-2xl bg-white/10 p-4 text-sm font-bold leading-relaxed text-white" role="status" aria-live="polite">{order.statusMessage ?? getCustomerOrderStatusMessage(order.status) ?? order.integrationMessage ?? 'Pedido recebido pela loja. Acompanhe a atualização nesta página.'}</p>{statusNotice && <p className="mt-3 rounded-2xl bg-[#d7f04a] p-3 text-sm font-black text-[#351924]" role="status" aria-live="polite">{statusNotice}</p>}<div className="mt-8 grid gap-4 sm:grid-cols-[1fr_auto] sm:items-center"><div><p className="text-xs font-bold uppercase tracking-wider text-white/55">Previsão</p><p className="mt-1 text-lg font-black">{estimate.label}</p><p className="mt-1 text-xs text-white/60">{estimate.detail}</p></div><span className="rounded-2xl bg-white/10 px-4 py-3 text-center text-xs font-bold text-white/70">{order.fulfillment.mode === 'PICKUP' ? 'Retirada na loja' : 'Entrega'}</span></div><div className="mt-8 grid gap-1" style={{ gridTemplateColumns: `repeat(${steps.length}, minmax(0, 1fr))` }}>{steps.map((step, index) => { const Icon = step.icon; const done = activeIndex >= index && order.status !== 'CANCELLED'; return <div key={step.label} className="text-center"><span className={`mx-auto grid size-9 place-items-center rounded-full ${done ? 'bg-[#d7f04a] text-[#351924]' : 'bg-white/10 text-white/35'}`}><Icon className="size-4" /></span><span className={`mt-2 block text-[10px] font-bold ${done ? 'text-white' : 'text-white/35'}`}>{step.label}</span></div>; })}</div>{order.status === 'CANCELLED' && <div className="mt-6 rounded-2xl bg-red-400/15 p-4 text-sm font-bold text-red-100">Este pedido foi cancelado. Entre em contato com a loja se precisar de ajuda.</div>}</div><p className="mt-4 rounded-2xl border border-[#82204f]/10 bg-white p-4 text-sm font-bold text-[#6f5360]">Guarde este código até o pedido ser concluído: <span className="text-[#82204f]">{orderCode}</span></p><section className="mt-4 rounded-[28px] bg-white p-6 shadow-sm"><div className="flex items-center justify-between"><h2 className="text-xl font-black">Resumo</h2><strong className="text-xl font-black text-[#82204f]">{formatBRL(displayOrder.pricing.totalCents)}</strong></div><div className="mt-5 space-y-4">{displayOrder.items.map((item, index) => <div key={index} className="border-t border-[#82204f]/8 pt-4 first:border-0 first:pt-0"><div className="flex justify-between gap-3"><strong>{item.quantity}x {item.productName}</strong><span className="text-sm font-bold">{formatBRL(item.totalPriceCents)}</span></div><p className="mt-1 text-xs text-[#826a75]">{item.sizeLabel}</p>{item.modifierSelections.filter((group) => group.items.length).map((group) => <p key={group.groupId} className="mt-1 text-xs text-[#826a75]">{group.groupName}: {group.items.map((selected) => `${selected.quantity > 1 ? `${selected.quantity}x ` : ''}${selected.name}`).join(', ')}</p>)}</div>)}</div></section><div className="mt-6 flex flex-col gap-3 sm:flex-row"><Button variant="outline" className="h-12 flex-1 rounded-full" onClick={load}><RefreshCw /> Atualizar status</Button>{config.whatsappEnabled && config.whatsappNumber && <Button className="h-12 flex-1 rounded-full bg-[#1f9d55] text-white hover:bg-[#178447]" render={<a href={whatsappUrl()} target="_blank" rel="noreferrer" />}><MessageCircle /> Abrir WhatsApp</Button>}</div><p className="mt-5 text-center text-xs text-[#826a75]">{updated ? `Última atualização em ${updated}. ` : ''}O código não permite listar outros pedidos.</p></div></main>;
}
